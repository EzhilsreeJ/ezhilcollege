document.addEventListener('DOMContentLoaded', () => {const buttons = document.querySelectorAll('nav button');const panels = document.querySelectorAll('main .panel');function showPanel(panelId) {panels.forEach(panel => {panel.classList.remove('active');});document.getElementById(panelId).classList.add('active');}buttons.forEach(button => {button.addEventListener('click', (event) => {const panelId = event.target.id.replace('-btn', '-panel');showPanel(panelId);});});showPanel('chat-panel');});